import React from 'react'
import footercss from '../styles/footer.module.scss'

const Footer = ({menuData, linkObject}) => {
  return (
    <div className={footercss.footermaincontainer}>
      hello this is the footer
    </div>
  )
}

export default Footer